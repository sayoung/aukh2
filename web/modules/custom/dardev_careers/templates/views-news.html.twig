    <!-- TESTIMONIAL AREA START (testimonial-3) -->
    <div class="ltn__testimonial-area bg-image pt-115 pb-70" data-bs-bg="img/bg/8.jpg">
        <div class="container">
            <div class="row">
                <div class="col-lg-12">
                    <div class="section-title-area ltn__section-title-2--- text-center---">
                        <h6 class=""><a href="/tous-les-actualites">+ D'Actualites</a></h6>
                        <h1 class="section-title">{{ view.getTitle()|raw }}</h1>
                    </div>
                </div>
            </div>
            <div class="row ltn__testimonial-slider-2-active slick-arrow-3">
                 {% for key, row in rows %}
                <div class="col-lg-12">
                    <div class="ltn__testimonial-item ltn__testimonial-item-3">
                        <div class="ltn__testimonial-img">
                            <img src="{% if row.image %}{% apply spaceless %}{{row.image | render | striptags}}{% endapply %}{% endif %}" alt="{% apply spaceless %}{{row.title | render | striptags | raw}}{% endapply %}">
                        </div> 
                        <div class="ltn__testimoni-info">
                            <p>
                                {% apply spaceless %}{{row.title | render | striptags | raw}}{% endapply %}
                            </p>
                            
                        </div>
                    </div>
                </div>
                {% endfor %}
                <!--  -->
            </div>
        </div>
    </div>
    <!-- TESTIMONIAL AREA END -->




















